description: |
  Simple collection of GPIOs for simple things

compatible: "ll,generic-gpios"

include: base.yaml

properties:
  input-gpios:
    type: phandle-array
  input-gpio-names:
    type: string-array
  output-gpios:
    type: phandle-array
  output-gpio-names:
    type: string-array

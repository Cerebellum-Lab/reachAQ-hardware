description: |
  Analog output using DAC peripheral.

compatible: "ll,analog-out"

include:
  - name: base.yaml

properties:
  io-channels:
    type: phandle-array
    required: true
    description: DAC peripheral instance and channel for outputting analog output
  
  resolution:
    type: int
    required: true
    enum: [8, 10, 12]
    default: 12
    description: DAC resolution, in bits

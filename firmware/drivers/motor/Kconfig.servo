menuconfig LL_SERVO
        bool "Support LeafLabs servo driver"
        default y
        depends on DT_HAS_LL_SERVO_ENABLED
        select USE_STM32_LL_TIM
        select DMA
        help
            Enable support for the servo driver

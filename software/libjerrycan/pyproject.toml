[build-system]
requires = [
    "setuptools",
    "setuptools-scm",
    "wheel",
    "ninja",
    "cmake",
    "mypy",
    "pytest",
]

build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
version_file = "_version.py"
root = "../.."

[project]
name = "pyjerrycan"
dynamic = ["version"]
